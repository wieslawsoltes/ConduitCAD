import { writeFile } from 'node:fs/promises';
import { ICON_NAMES, ICON_GROUPS, icon } from '../packages/icons/src/index.js';
const title = s => s.replaceAll('-', ' ');
const body = Object.entries(ICON_GROUPS).map(([group,names])=>`<section><h2>${group}</h2><div class="grid">${names.map(name=>`<div class="item" data-search="${name} ${group.toLowerCase()}">${icon(name)}<span>${title(name)}</span><code>${name}</code></div>`).join('')}</div></section>`).join('');
await writeFile(new URL('../docs/icon-atlas.html', import.meta.url), `<!doctype html><html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Conduit CAD · Icon atlas</title><style>body{font:15px system-ui;margin:0;padding:24px;color:#173e37;background:#f5f9f7}main{max-width:1160px;margin:auto}h1{font-size:32px;margin-bottom:10px}p{line-height:1.7}input{font:inherit;border:1px solid #b7d0c7;padding:12px;border-radius:8px;box-sizing:border-box;width:100%}h2{margin:28px 0 16px}.grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(138px,1fr));gap:10px}.item{min-height:125px;background:white;border:1px solid #d3e4dc;border-radius:10px;display:flex;align-items:center;justify-content:center;gap:9px;flex-direction:column;padding:12px}.icon{width:32px;height:32px}code{font-size:11px;color:#597569}.item[hidden],section[hidden]{display:none}</style></head><body><main><h1>Conduit / Iconography</h1><p>${ICON_NAMES.length} original, scalable UI glyphs. A shared 24-unit grid, rounded strokes and restrained surface accents. These are interface affordances, not ISO/IEC drawing symbols.</p><label>Find an icon<input id="search" type="search" placeholder="Extrude, hole, dimensions, files…"></label><div id="groups">${body}</div></main><script>document.querySelector('#search').addEventListener('input',e=>{const words=e.target.value.toLowerCase().split(/\\s+/).filter(Boolean);for(const item of document.querySelectorAll('.item'))item.hidden=!words.every(w=>item.dataset.search.includes(w));for(const group of document.querySelectorAll('section'))group.hidden=![...group.querySelectorAll('.item')].some(i=>!i.hidden);});</script></body></html>`);
console.log(`Wrote ${ICON_NAMES.length} glyphs to docs/icon-atlas.html`);
